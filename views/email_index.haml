%h1
  Your six month reminders past and present
  
  %ul
  - @emails.each do |email|
    %li
      %a{:href => "emails/#{email.id}", :class => 'email_link'}
        View email due to be sent on
        = email.send_on